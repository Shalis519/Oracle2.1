Текущий прогноз выглядит так:

Сейчас ты стоишь на развилке между "Общение" и "Учёба".

Энергия течёт свободно и естественно. То, что задумано, получится легко.

Твой выбор определит, как Общение трансформируется в Учёба.

У тебя есть в ближайшие 4 часа, чтобы выбрать новую ветку реальности.

Идея, которая придет в моменте, когда Общение встретится с Учёба (запомни это время: 5 июля в 00:00), — это не про сегодня.

Это про твой следующий год.

Если ты её проигнорируешь, мир не рухнет.

Но если запишешь и начнёшь делать — через в ближайшие 4 часа ты не узнаешь свою жизнь.

У тебя есть в ближайшие 4 часа, чтобы сделать выбор в пользу Общение.

Действуй прямо сейчас! Время на исходе.

После этого окно захлопнется.

Не потому что возможности исчезнут, а потому что ты войдёшь в другой сценарий,

где этот выбор уже неактуален.

Что бы ты делал, если бы знал, что не можешь провалиться?

Ключевые транзиты сегодня
Меркурий → Уран (Тригон, орб 0.2°)
Венера → Уран (Квадрат, орб 0.3°)
Луна → Марс (Оппозиция, орб 0.4°)
Солнце → Марс (Секстиль, орб 0.6°)

Нужно заменить шаблонные фразы на более естественные.

---

## ЗАДАЧА 1: Таблица мотивационных фраз (БД)

Создать таблицу для хранения мотивационных фраз:

```sql
CREATE TABLE motivation_phrases (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  phrase TEXT NOT NULL,
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT NOW()
);
Начальные фразы (засеять в БД):

sql
INSERT INTO motivation_phrases (phrase) VALUES
  ('Действуйте, у Вас всё получится!'),
  ('Пришло время брать инициативу в свои руки!'),
  ('Как говорил Гагарин: «Поехали!»'),
  ('Ваш момент — сейчас. Действуйте!'),
  ('Время пришло. Не откладывайте.'),
  ('Верьте в себя. Вы справитесь!'),
  ('Сделайте этот шаг — и мир изменится.'),
  ('Вы способны на большее. Начните!'),
  ('Это Ваш звёздный час. Вперёд!');
ЗАДАЧА 2: API для управления фразами (админ)
В routes/adminOntology.ts добавить эндпоинты:

typescript
// GET /admin/motivation-phrases — получить все фразы
// POST /admin/motivation-phrases — добавить фразу
// DELETE /admin/motivation-phrases/:id — удалить фразу
// PATCH /admin/motivation-phrases/:id/toggle — включить/отключить
ЗАДАЧА 3: UI в Oracle Studio
В admin-studio.tsx добавить раздел «Мотивационные фразы»:

Список всех фраз с чекбоксами is_active

Кнопка «Добавить фразу» (поле ввода)

Кнопка «Удалить» рядом с каждой фразой

ЗАДАЧА 4: Замена шаблонных фраз в прогнозе
В futuristicGenerator.ts в buildText() заменить:

4.1. Вместо "выбрать новую ветку реальности"
Использовать случайный вариант:

typescript
const actionPhrases = [
  'сделать выбор, который изменит Ваш путь',
  'принять решение, которое определит направление',
  'сделать шаг в сторону перемен',
  'выбрать новый курс'
];
const actionPhrase = actionPhrases[Math.floor(Math.random() * actionPhrases.length)];
4.2. Вместо "это не про сегодня. Это про твой следующий год"
Использовать случайный вариант. {futurePeriod} — вычисляется из эфемериды:

typescript
const futurePhrases = [
  `То, что Вы решите сейчас, откликнется через ${futurePeriod}.`,
  `Этот выбор повлияет на Вашу жизнь в ${futurePeriod}.`,
  `Сегодняшнее решение станет фундаментом для перемен в ${futurePeriod}.`,
  `Последствия этого выбора Вы почувствуете в ${futurePeriod}.`
];
const futurePhrase = futurePhrases[Math.floor(Math.random() * futurePhrases.length)];
4.3. Вместо "После этого окно захлопнется..."
Убрать эту фразу. Вместо неё — мотивационная фраза из БД:

typescript
// Получить случайную активную фразу из БД
const phrases = await db.select()
  .from(motivationPhrases)
  .where(eq(motivationPhrases.isActive, true));

const randomPhrase = phrases[Math.floor(Math.random() * phrases.length)];
// Добавить в конец прогноза
parts.push(randomPhrase.phrase);
4.4. Убрать дублирование "У Вас есть X..."
Оставить только одно упоминание в блоке «Таймер». Убрать из «Бифуркации» и «Окна возможностей».

4.5. Вместо "Твой выбор определит, как Общение трансформируется в Учёба"
Использовать случайный вариант:

typescript
const choicePhrases = [
  `Ваш выбор: остаться в привычном ${primaryTheme.toLowerCase()} или шагнуть в ${secondaryTheme.toLowerCase()}.`,
  `Сейчас Вы выбираете между ${primaryTheme.toLowerCase()} и ${secondaryTheme.toLowerCase()}.`,
  `Что для Вас важнее: ${primaryTheme.toLowerCase()} или ${secondaryTheme.toLowerCase()}?`
];
const choicePhrase = choicePhrases[Math.floor(Math.random() * choicePhrases.length)];
4.6. Склонение темы (Учёба → Учёбой)
Добавить функцию склонения:

typescript
function declineTheme(theme: string): string {
  const exceptions: Record<string, string> = {
    'Учёба': 'Учёбой',
    'Любовь': 'Любовью',
    'Творчество': 'Творчеством',
    'Общение': 'Общением',
    'Свобода': 'Свободой',
    'Гармония': 'Гармонией',
    'Красота': 'Красотой',
    'Отношения': 'Отношениями',
    'Деньги': 'Деньгами'
  };
  return exceptions[theme] || theme;
}
ИТОГОВЫЙ ПРИМЕР ПОСЛЕ ВСЕХ ЗАМЕН
text
Сейчас Вы стоите на развилке между "Общение" и "Учёба".

Энергия течёт свободно и естественно. То, что задумано, получится легко.

Ваш выбор: остаться в привычном общении или шагнуть в учёбу.

У Вас есть 4 часа, чтобы сделать выбор, который изменит Ваш путь.

Идея, которая придет в момент, когда Общение встретится с Учёбой (пик аспекта: 5 июля в 14:30), — это не случайность.

То, что Вы решите сейчас, откликнется через несколько месяцев.

Если Вы её проигнорируете, мир не рухнет.

Но если запишете и начнёте делать — через этот же период Вы не узнаете свою жизнь.

Действуйте, у Вас всё получится!

Ключевые транзиты сегодня
Меркурий → Уран (Тригон, орб 0.2°)
Венера → Уран (Квадрат, орб 0.3°)
Луна → Марс (Оппозиция, орб 0.4°)
Солнце → Марс (Секстиль, орб 0.6°)
ИТОГОВЫЙ СПИСОК ЗАДАЧ
№	Задача	Файл
1	Создать таблицу motivation_phrases	БД
2	Засеять начальные фразы	БД
3	API для управления фразами	routes/adminOntology.ts
4	UI в Oracle Studio	admin-studio.tsx
5	Заменить "выбрать новую ветку реальности"	futuristicGenerator.ts
6	Заменить "это не про сегодня..."	futuristicGenerator.ts
7	Убрать "После этого окно захлопнется..." (вместо неё — фраза из БД)	futuristicGenerator.ts
8	Убрать дублирование "У Вас есть X..."	futuristicGenerator.ts
9	Заменить "Твой выбор определит..."	futuristicGenerator.ts
10	Добавить склонение темы	futuristicGenerator.ts
11	Использовать реальный peakTime из эфемериды	futuristicGenerator.ts
12	Использовать real futurePeriod из эфемериды	futuristicGenerator.ts
ВАЖНО: Обращение
Во всех новых фразах использовать «Вы, Ваш, Ваше, Ваши», а не «ты». Это уже работает в прогнозах, просто убедитесь, что новые фразы соответствуют этому стилю.